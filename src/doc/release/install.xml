<doc>
  <head>
    <title>Grammatica Installation Instructions</title>
  </head>

  <body>

    <h1>Requirements</h1>

    <p>The following software is required in order to run 
    Grammatica:</p>

    <list>
      <item>
        <text>Java runtime compatible with JDK 1.5.</text>
      </item>
    </list>

	<p>To use the parsers generated by Grammatica, one of the
	following software packages are required (depending on the
	language generated):</p>
	
    <list>
      <item>
        <text>Java runtime compatible with JDK 1.5, for Java 
        parsers.</text>
      </item>

      <item>
        <text>Mono 2.0 (or another .NET 2.0 compatible), for C#
        or VisualBasic parsers.</text>
      </item>
    </list>


    <h1>Installation</h1>

    <p>This software does not require any special installation. The only
    file needed is the <code>lib/grammatica-<param name="version"/>.jar</code> 
    file.</p>


    <h1>Running from Command-Line</h1>

    <p>The following command runs Grammatica and prints the 
    command-line options:</p>

    <pre>java -jar lib/grammatica-<param name="version"/>.jar</pre>


    <h1>Running from Apache Ant</h1>

    <p>Grammatica is also available as an Apache Ant task. Add the 
    <code>grammatica-<param name="version"/>.jar</code> to the 
    <code>CLASSPATH</code>, and add the following to include the
    Grammatica task:</p>
    
    <pre>&lt;taskdef name="grammatica"
         classname="net.percederberg.grammatica.ant.GrammaticaTask" /&gt;</pre>

    <p>See the reference manual for the details on how to use the
    <code>&lt;grammatica&gt;</code> task.</p>


    <h1>Running Generated Parsers</h1>

    <p>All generated parsers require the corresponding Grammatica 
    run-time library in order to run. The run-time libraries for 
    various languages can be found in the <code>lib</code> 
    subdirectory:</p>

    <list>
      <item>
        <text><code>grammatica-<param name="version"/>.jar</code>
        for Java parsers</text>
      </item>

      <item>
        <text><code>grammatica-<param name="version"/>.dll</code>
        for C# or VisualBasic parsers</text>
      </item>
    </list>
    
  </body>
</doc>
